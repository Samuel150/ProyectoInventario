name: Testeo
on: [push]

jobs:
  prueba:
    name: Testeo PHP
    runs-on: ubuntu-latest
    steps:
      - name: PHP Syntax Checker (Lint)
        uses: StephaneBour/actions-php-lint@7.3
        with:
          dir: 'Inventarios'
